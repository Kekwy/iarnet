syntax = "proto3";

package iarnet.provider;

import "iarnet/actor/actor.proto";

option java_package = "com.kekwy.iarnet.proto.provider";
option java_multiple_files = true;

service ActorRegistrationService {
  rpc actorChnannel(stream actor.ActorEnvelope) returns (stream actor.ActorEnvelope);
}
