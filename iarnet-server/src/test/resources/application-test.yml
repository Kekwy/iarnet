# 测试用配置：MinIO API 使用本地 9002 端口
iarnet:
  oss:
    enabled: true
    endpoint: http://localhost:9002
    access-key: minioadmin
    secret-key: minioadmin
    bucket: iarnet-artifacts-test
    presigned-expiry-seconds: 60
